import styled from 'styled-components'

export const Headers = styled.header`
  color: var(--text);
  width:100%;
  position: absolute;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 0.75rem;
`
